apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  type: NodePort
  selector:
    app: {{ include "frontend.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
